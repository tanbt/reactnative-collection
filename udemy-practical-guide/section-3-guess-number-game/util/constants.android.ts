// Rename the file with postfix `*.android.ts` or `*.ios.ts` to use it for 
// a specific platform. The import statement remains unchanged.

export enum Colors {
  primary500 = "#527206",
  primary600 = "#640200",
  accent500 = "#ddb500",
}
