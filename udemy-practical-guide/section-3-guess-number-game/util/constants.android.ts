// Rename the file with postfix `*.android.ts` or `*.ios.ts` to use it for
// a specific platform.

export enum Colors {
  primary500 = "#72063c",
  primary600 = "#640233",
  accent500 = "#ddb52f",
}
