# Project
Although this project use Expo SDK 55, it will be deployed as `my-expo-54-app`, which means its EAS (Expo project id, etc) is the same,
as the app `my-expo-54-app` is already deployed and we want to reuse it instead of creating a new app and go through Apple review process, etc.

